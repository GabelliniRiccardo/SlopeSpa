DTO:
  SPADTO:
    NumbersOnly: Insert numbers only
  UserDTO:
    Name:
      LettersOnly: Name can not contain numbers
    LastName:
      LettersOnly: Last name can not contain numbers
    Password:
      PasswordFieldMustMatch: The password fields must match
  OperatorDTO:
    NumbersOnly: Insert numbers only
  CustomerDTO:
      NumbersOnly: Insert numbers only
